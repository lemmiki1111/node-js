<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>get CRUD</title>
    <link href="/css/style.css" rel="stylesheet" />
  </head>
  <body>
    <h1 class="text-center font-bold text-4xl mt-2 text-blue-300">
      THÔNG TIN USER
    </h1>
    <div class="p-6">
      <div class="overflow-x-auto rounded-2xl shadow-lg">
        <table class="min-w-[700px] w-full border-collapse">
          <thead>
            <tr
              class="bg-gray-100 text-gray-700 text-sm font-semibold divide-x divide-gray-300"
            >
              <th class="px-4 py-3 text-left">Email</th>
              <th class="px-4 py-3 text-left">First name</th>
              <th class="px-4 py-3 text-left">Last name</th>
              <th class="px-4 py-3 text-left">Address</th>
              <th class="px-4 py-3 text-left">Action</th>
            </tr>
          </thead>
          <tbody class="text-sm text-gray-800 divide-y divide-gray-200">
            <%for (let i=0;i < dataTable.length; i++){%>
            <tr
              class="hover:bg-indigo-100 transition-colors divide-x divide-gray-200 focus:bg-indigo-500 "
            >
              <td class="px-4 py-3"><%=dataTable[i].email%></td>
              <td class="px-4 py-3 font-medium"><%=dataTable[i].firstName%></td>
              <td class="px-4 py-3"><%=dataTable[i].lastName%></td>
              <td class="px-4 py-3"><%=dataTable[i].address%></td>
              <td>
                <a
                  href="/edit-crud?id=<%=dataTable[i].id%>"
                  class="bg-yellow-400 no-underline hover:bg-yellow-500 text-white font-medium px-3 py-1 rounded-lg shadow inline-block"
                >
                  Edit
                </a>

                <a
                  href="/delete-crud?id=<%=dataTable[i].id%>"
                  class="bg-red-500 hover:bg-red-600 no-underline text-white font-medium px-3 py-1 rounded-lg shadow inline-block"
                >
                  Delete
                </a>
              </td>
            </tr>

            <%}%>
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
